version: 2
jobs:
  build:
    docker:
      - image: circleci/node:6.11
    steps:
      - checkout
      - run:
          name: Install npm
          command: npm install
    test:
      - run:
          name: CoPilot OSS
          command: bash <(curl -s https://copilot.blackducksoftware.com/ci/circle/scripts/upload)
      - run:
          name: test
          command: npm test
